// Valida o link geral no servidor antes de exibir dados e formulário do evento.
import { createClient } from "@supabase/supabase-js"; import { notFound } from "next/navigation"; import { GeneralForm } from "./GeneralForm"; import type { Metadata } from "next"; import Image from "next/image";
export const dynamic="force-dynamic";
export const metadata:Metadata={title:"Cadastro de convidado | Braza",description:"Cadastre até duas pessoas no churrasco.",openGraph:{title:"Cadastro de convidado | Braza",description:"Confirme sua presença.",images:[]},twitter:{title:"Cadastro de convidado | Braza",description:"Confirme sua presença.",images:[]}};
export default async function GeneralRegistration({params}:{params:Promise<{token:string}>}){const{token}=await params;const admin=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!,{auth:{persistSession:false}});const{data:event}=await admin.from("events").select("title,event_date,address").eq("invite_token",token).eq("status","active").maybeSingle();if(!event)notFound();return <main className="public-shell invite-shell"><section className="invite-card"><Image className="invite-logo" src="/braza-logo.png" alt="Braza" width={96} height={96}/><span className="eyebrow">CADASTRO DE CONVIDADO</span><h1>{event.title}</h1><p className="invite-intro">Cadastre você e, se quiser, um acompanhante.</p><div className="event-details"><span><b>{new Date(event.event_date).toLocaleDateString("pt-BR")}</b>Data do churrasco</span><span><b>{event.address||"Local a confirmar"}</b>Endereço</span></div><GeneralForm token={token} eventTitle={event.title} eventDate={event.event_date} address={event.address}/><small className="privacy">🔒 Limite de duas pessoas por cadastro.</small></section></main>}
